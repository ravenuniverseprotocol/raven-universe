services:
  - type: web
    name: raven-universe
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.10.0
    autoDeploy: true
